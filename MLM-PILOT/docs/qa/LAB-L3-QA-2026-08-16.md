# Comp Plan Lab, Phase L3, Quality Assurance (QA) Acceptance Gate, 2026-08-16

Grader: mlm-qa. Scope: commit d25a2ef (8 files). Spec of record:
`docs\COMP-LAB-SPEC.md` version 1.3, sections 9 (scenarios), 10 (watched
accounts), 7 (L3 deliverables), and the section 8 defaults 7 through 11.
Builder evidence: `docs\verification\LAB-L3-PROOF-RUN-2026-08-16.md`. The
mlm-verifier concurrently replays the money (S1, S2, the stack, ordinals,
roll-up); this gate does not duplicate that. QA fixes nothing.

CHECKLIST DISCIPLINE: built from the spec BEFORE any deliverable was opened.
Evidence: file reads at d25a2ef, git history, live read-only queries plus one
QA-authored tamper probe on the cloud project (oiyibdczkokegaxkwulv), and
outside requests against the live site and the anonymous REST surface.

Acronym key: Quality Assurance (QA), Multi-Level Marketing (MLM), Structured
Query Language (SQL), Sales Volume (SV), Commissionable Volume (CV), Team
Volume (TV), Group Volume (GV), JavaScript Object Notation (JSON),
Representational State Transfer (REST), HyperText Transfer Protocol (HTTP),
Message Digest 5 (MD5).

## VERDICT: PASS (one MEDIUM defect, three LOW items; no missing deliverable)

Every L3 deliverable the spec promises exists at the promised name and
shape, every traced documentation claim held, the walls held against a fresh
independent tamper, and the lab remains invisible from outside. The MEDIUM is
a now-false gotcha line in the lab README that this same commit edited
elsewhere; it contradicts the phase's own headline feature and should be
corrected in the next commit that touches the README (QA fixes nothing).

---

## A. Spec conformance (L3 deliverables)

| # | Promise (spec cite) | Evidence | Verdict |
|---|---|---|---|
| A1 | Five mutation kinds with 9.2 semantics and exact kind names | `db\lab\014` fn_replay_mutations: add_member (synthetic under new_sponsor_ref, profile volume), remove_member (single set-based re-point to the target's sponsor, argued equivalent to the ascending walk in design decision 5; root REFUSED), remove_leg (recursive subtree delete; root refused), move_member (cycle refusal via subtree scan), set_volume (sv_override). Kind names exactly the 001 CHECK vocabulary ('set_volume') | PASS |
| A2 | Missing references FAIL loudly, never skipped (9.2) | Every kind raises "replay failed loudly: ..." naming the mutation ordinal, kind, scenario, and reference; live evidence: run id 52 is burned (absent between runs 51 and 53), the rolled-back root-removal probe | PASS |
| A3 | Deterministic replay (9.4): root-first effective list, id = 10,000,000 + ordinal, code = 'LAB-' + scenario_code + '-' + seq | fn_effective_mutations (recursive chain, deepest ancestor first, seq order); the id and code rules verbatim at 014 lines 198 to 199; live: LAB-S1-1 carries id 10,000,001; same-flow duplicate runs pay identically (44 = 57 = 72 at 46.00; 61 = 73 at 16.80; 71 = 74 at 26.00) | PASS |
| A4 | Name wall holds for replay synthetics (2.3) | Codes are 'LAB-' prefixed by construction; the 001 CHECK still guards any row without a census reference | PASS |
| A5 | Locking and stacking discipline; stack depth cap 3, depth 4 refused (9.2, default 7) | fn_validate_scenario_shape computes chain depth and raises past 3, fired by an AFTER trigger on the transition to locked; live scenarios S1X (parent S1, depth 2) and S-D3 (parent S1X, depth 3) locked; the depth-4 probe in `113` prints "PASS: depth-4 lock refused (cap 3)" and no depth-4 scenario exists | PASS |
| A6 | Watchlist live; cap 10 is a SOFT cap, warning only (10.1, default 8) | `015` warn_watchlist_cap raises a NOTICE (never an exception) past 10 active rows | PASS |
| A7 | Snapshot shape per 10.1 with the default-11 baseline | lab.watch_snapshots carries all eleven promised fields; plan-shaped shape_volume per plan (tv / left-right / three legs / gv-bracket); contributing_downline_count = distinct sources for per-source plans, basis member count for aggregate; baseline = named run else latest complete same-period identity unilevel; unresolvable baseline writes {"no_baseline": true} with null delta (24 such rows live, disclosed) | PASS |
| A8 | Six-bucket decomposition summing exactly; aggregate plans decomposable false with basis movements (10.2) | The six buckets computed by full-join over source CODES; aggregate part carries aggregate_delta, basis_before/after, basis_members_gained/lost sorted, and the label "basis movement, not attribution"; the invariant ABORTS the run on violation (015 line 384); live recomputation over all 64 snapshots: ZERO violations; live S1X row: delta 0.00 = +4.00 from_added_members - 4.00 from_level_shift, decomposable true | PASS |
| A9 | Month scoping (default 9) | Active-window check per mutation (month_from / month_to); live S-REM: July run 50 pays the identity 34.00 with five members, August run 51 pays 26.00 with M4 gone | PASS |
| A10 | Volume profiles in multiples of 50.00 (default 10) | Enforced twice: fn_profile_sv raises on non-multiples, and lock-time shape validation checks every profile value | PASS |
| A11 | Snapshots for every ACTIVE watched account present in the derived tree, every run (10.1) | fn_write_watch_snapshots loops active watchlist, skips absent members, runs inside fn_execute_plan while status is 'running' (the freeze admits no later write); 64 rows live across the L3 runs | PASS |

## B. Documentation truth (proof doc claims traced)

| # | Claim | Evidence | Verdict |
|---|---|---|---|
| B1 | S1 matches 9.5 (company 46.00, deltas +4.00 / +8.00 in from_added_members; binary unchanged at 24.00) | Live runs 44 (46.00, 2 paid) and 45 (24.00); the money-level replay is the verifier's row | PASS |
| B2 | S2 matches 10.3 (38.00 / 32.00; the four watch rows) | Live runs 46 (38.00, 3 paid) and 47 (32.00, 1 paid) | PASS |
| B3 | Stack S1X matched (50.00 unilevel, 40.00 binary A draft; the delta 0.00 teaching row) | Live runs 48 (50.00, 3 paid) and 49 (40.00, 2 paid); the S1X watch row live: delta 0.00 composed of +4.00 added and -4.00 level-shift, exactly as told | PASS |
| B4 | The check-side correction disclosed honestly (first flush check summed the per-member metric over everyone, 440.00; corrected to the line-scoped sum; engine unchanged) | The disclosure names its own error, its cause, and ties it to the standing lines-not-flags lesson (QA LOW-2 of L2); the corrected reading is the one the L2 gate already established as correct. Flush semantics arithmetic is the verifier's lane | PASS |
| B5 | Determinism: zero disagreements across 66 completed runs (semantic, code-keyed) plus byte-order on the three duplicate pairs | Live: exactly 66 completed runs; duplicate recipes pay identically (three pairs spot-checked); the disclosed id-keyed-digest incident (cross-convention fixture pairs flagged on ids while every value agreed) is coherent with the two base-id conventions being different by construction | PASS |
| B6 | Refused probes: depth-4 lock, root removal with full rollback | `113` prints both PASS notices (lines 195 and 221); run id 52 burned; scenario S-BAD-ROOT exists locked with its one mutation, its run absent | PASS |
| B7 | Run ledger | Live registry matches the sweep table run for run (35 to 74); runs 75 and 76 (V-ORD, V-REM2) are the CONCURRENT VERIFIER's own probe scenarios, created after the proof doc, correctly absent from it | PASS |
| B8 | Synthetic identity claims | LAB-S1-1 id 10,000,001 live in derived rows (the LAB-S1 row my sweep also caught is L2's PROOF-STAIR fixture member, a different namespace, not a code-rule violation) | PASS |
| B9 | Month-scope claims | Runs 50 / 51 as at A9 | PASS |
| B10 | Isolation, disclosed as CHANGED with attribution: app inventory no longer the L1 baseline, delta belongs to the subscriptions build (migrations 024 to 027), lab's own contribution zero three ways | Live: migration ledger shows subscription_engine_schema_024, _025, _026, bridge_covered_months_027 interleaved with comp_lab_l3_014/015; app RLS policies still 10; lab functions with app writes: 0; app-to-lab FKs: 0; lab-to-app FKs: exactly the one sanctioned; lab grants to public roles: 0. The doc's honesty holds: it claimed the change and attributed it, rather than claiming "unchanged" | PASS |

## C. Outside view (re-check)

| # | Row | Evidence | Verdict |
|---|---|---|---|
| C1 | Anonymous REST cannot reach schema lab (including the new table) | Fresh probe against lab.watch_snapshots with the public anon key: HTTP 406, PGRST106 "Invalid schema: lab" | PASS |
| C2 | Live site carries zero lab references | Fresh fetch of https://orvanna.io: no lab, scenario, watchlist, what-if, LAB-, or plan-comparison content | PASS |

QA's independent tamper probe: INSERT into lab.watch_snapshots against
completed run 31 REFUSED live by the child freeze ("run 31 is complete ...
immutable history"), proving the new table joined the L2 freeze regime.

## D. Hygiene (8 committed files)

| # | Row | Evidence | Verdict |
|---|---|---|---|
| D1 | No em or en dashes | Unicode scan: zero hits across all 8 | PASS |
| D2 | Acronym keys | All 8 files keyed | PASS |
| D3 | No Unicity terminology, personal data, secrets | grep: zero hits | PASS |
| D4 | Never-edit-applied discipline | d25a2ef touches no file 001 through 013; 014/015 are new numbered files; applied-migration ledger names match the repository files | PASS |

## E. Status claims

| # | Row | Evidence | Verdict |
|---|---|---|---|
| E1 | Nothing overstates L3 | README line 10: "L3 gates pending"; proof doc closes "Phase L3 remains OPEN until mlm-verifier and QA say so"; no dashboard file exists anywhere; the only dashboard mentions are forward-looking L4 obligations | PASS |
| E2 | Lab README truthful for L3 | Updated header, file table rows 014/015 and 111 to 114 accurate against the files, EXCEPT one stale gotcha, defect MEDIUM-1 below | PASS with MEDIUM-1 |
| E3 | Fifth-plan registration does not leak into L3 claims | 'orvanna_builder' (registered in the spec by ee9f3eb, specified in its own document) appears nowhere in the L3 files or claims; the lab dispatcher still covers exactly the four launch plans, per spec | PASS |

---

## Defects

**MEDIUM-1: a now-false gotcha in the lab README.** The gotcha list still
says: "Scenario mutations are phase L3: any scenario whose effective list
carries a mutation fails `lab.fn_derive_members` loudly. Only IDENTITY (and
the hand-built proof fixtures) run in L1." After file 014, mutations REPLAY;
that sentence describes the retired L1 placeholder as present-tense behavior
and contradicts the phase's own headline feature, in a file this very commit
edited elsewhere. The neighboring fixture gotcha ("derived sets are inserted
by the proof scripts, not replayed") is also half-stale: since L3, the S1/S2
mini fixtures DO replay over a loaded base. Correct both in the next README
commit. (Charter documentation-truth rule applied; ranked MEDIUM rather than
HIGH because it is a stale behavior note, not a false phase-status claim.)

**LOW-1: fn_run_plan's catalog signature changed again without spec
ratification.** 015 drops the five-parameter function (the v1.3 "signature of
record") and creates the six-parameter one with a defaulted
p_baseline_run_id. Every five-argument call still behaves exactly as
ratified, so the callable contract holds; the builder flagged it for the same
ratification the fifth parameter received. Architect item for v1.4, with the
lock-time-validation scoping sentence (builder flag 1).

**LOW-2: re-baseline the app inventory before L4.** The
zero-new-app-objects claim now needs a joint baseline number signed by both
the lab and subscriptions builders (the proof doc recommends this; this gate
endorses it).

**LOW-3 (carried forward): the L2 LOWs stand where not yet fixed** (the
lines-not-flags dashboard rule and the residue display rule are STANDING L4
obligations; file 013's unexpanded "QA" remains).

## Verdict, restated

PASS. All eleven conformance rows, all ten documentation-truth rows, both
outside-view rows, and all hygiene and status rows pass; the one MEDIUM is a
stale README gotcha, correctable in any next README commit and not blocking.
Notable in this phase's evidence: the component invariant held over all 64
live snapshots under independent recomputation, the burned run id 52 silently
corroborates the loud-rollback story, and the S1X watch row (delta 0.00 =
+4.00 - 4.00) is live and exact. Phase L3 closes when the mlm-verifier's
verdict is also PASS.
