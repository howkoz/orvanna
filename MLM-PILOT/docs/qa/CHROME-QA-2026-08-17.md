# Corporate chrome gate: quality assurance verdict

**Artifact graded:** commit `713e3f8`, built to `MLM-PILOT\deploy\dist` and served over
Hypertext Transfer Protocol (HTTP) on `127.0.0.1:8322`.
**Law:** `MLM-PILOT\docs\CORPORATE-CHROME-CONTRACT.md`
**Graded by:** mlm-qa, 2026-08-17. The builders graded nothing here.

**VERDICT: FAIL. DEPLOY: NO.**

No HIGH defect. Three MEDIUM and five LOW. The gate is withheld because one of the
nine pages still renders the navigation bar at a different size, spacing and alignment
from the other eight, which is Howard's original complaint surviving on the Plan page,
and because the one acceptance line that says the Support button *works* could not be
evidenced in this environment on any page.

Acronym key: quality assurance (QA), Cascading Style Sheets (CSS), JavaScript (JS),
Hypertext Markup Language (HTML), Scalable Vector Graphics (SVG), Web Content
Accessibility Guidelines (WCAG), success criterion (SC), Personal Volume (PV).

---

## 1. How this was graded

The Browser pane was not compositing frames in this session, so no screenshot was
possible. Every visual claim below is therefore a computed measurement taken from
rendered pages in a real browser, not a reading of source. Contrast was computed from
`getComputedStyle`, compositing every translucent background layer down the ancestor
chain to the first opaque layer, resolving inherited element opacity, and averaging the
colour stops of any opaque gradient background.

Two measurement errors of my own were caught and corrected before anything was
reported, and they are recorded here so the numbers can be trusted:

1. **Transition lag.** Flipping `data-theme` and reading immediately returns
   mid-transition colours. A first pass produced ratios of 1.10 to 1.69 on
   `faq.html` and `conductor.html` that were pure artefact. Every figure in this
   report comes from a page **loaded fresh with the target theme already in storage**
   and read after a settle delay.
2. **Gradient blindness.** An early pass ignored `background-image`, which made the
   bank-connect dialog on `shop.html` look catastrophic (title at 1.65 to 1, progress
   labels at 1.21 to 1). The dialog card is painted by an opaque white gradient. With
   gradients resolved, the same elements measure 17.45 and 12.66. **Those early
   figures are withdrawn and are not findings.**

Disabled controls are excluded from the pass or fail decision, per WCAG SC 1.4.3,
but are reported where measured.

---

## 2. Acceptance checklist

Built from the contract's section 7 definition of done plus the standing checklist
rows, before looking at the deliverable.

| # | Promise (contract reference) | Evidence | Result |
|---|---|---|---|
| 1 | All nine corporate pages carry the canonical navigation, character for character, except the active item and the cart (7.1, 2) | Walked index, then clicked every navigation item in turn, then reached faq from the footer, product from a shop tile, library-agent from a library tile. Rendered item list identical on all nine: Learn, Shop, Library, Plan, Conductors, Team, Support, Enroll (soon), Sign In, theme. Cart appended after the theme button on shop and product only. | PASS |
| 2 | Item order never changes between pages | Rendered order identical on all nine at 1280, 1024, 768 and 390 pixels wide | PASS |
| 3 | No item hidden or unreachable on any page | Zero items with `display:none`, `visibility:hidden` or zero width on any page at any of the four widths | PASS |
| 4 | Every item is hit testable, nothing overlays it | `document.elementFromPoint` at each item centre resolves to that item on every page | PASS |
| 5 | Active item is the page's own, with `aria-current="page"` (2, rule 1) | index=Learn, shop=Shop, product=Shop, team=Team, comp-plan=Plan, conductor=Conductors, library=Library, library-agent=Library, faq=none (faq is not a navigation item) | PASS |
| 6 | Brand block href is index.html everywhere (2, rule 2) | Confirmed on all nine | PASS |
| 7 | One icon-only theme control on all nine (7.2, 3) | Present on all nine, exactly one visible icon at a time, sun on dark and moon on light | PASS |
| 8 | No visible Light or Dark text anywhere on any page | Scan of every leaf element on all nine, both themes, for visible text matching light, dark, auto or theme: zero hits | PASS |
| 9 | Accessible name tracks state (3) | "Switch to the light theme" while dark, "Switch to the dark theme" while light, verified on all nine and across a toggle in both directions | PASS |
| 10 | Hit target at least 40 by 40 pixels (3) | 40 by 40 on all nine at 1280, 1024, 768 and 390 pixels | PASS |
| 11 | Works in both directions | Clicked dark to light to dark to light on library-agent and index: attribute, storage, accessible name and icon all track | PASS |
| 12 | Survives reload | Chose light, reloaded index: still light | PASS |
| 13 | Follows the reader across pages under one key (3) | Chose light on index by clicking the icon, then clicked through to comp-plan and on to library: light held on all three, storage key `orvanna-theme` = light | PASS |
| 14 | Old storage keys retired (3) | Four keys existed before (`orvannaLibraryTheme`, `orvanna-conductor-theme`, `orvanna-comp-plan-theme`, `orvanna-faq-theme`). None is read or written anywhere in the build; they survive only in a comment | PASS |
| 15 | Dark stays the default (3) | With storage cleared, first visit to index renders dark | PASS |
| 16 | No flash of the wrong theme on load (3) | Every page carries a synchronous inline script in `<head>` that sets `data-theme` before `<body>` exists. Light values are expressed only as `:root[data-theme="light"]` overrides, so no wrong-theme paint is possible | PASS |
| 17 | A light theme that genuinely renders on all nine, contrast computed and reported (7.3, 5) | Full sweep of every text-bearing element, both themes, all nine pages, gradients resolved, disabled excluded: **zero pairs below the floor in either theme**. Section 4 has the numbers | PASS |
| 18 | Surfaces that cannot be light are kept dark deliberately and documented (5) | Hero, chrome bar, footer, bank dialog and wallet sheet all keep dark treatment; documented in `corporate.css` and `shop.css` | PASS with a disclosure gap, finding M3 |
| 19 | The Support button works on all nine (7.4) | Trigger present on all nine, handler wired on all nine (spy on the vendor open call fires once per click on every page). **The chat panel itself never rendered in this environment on any page, including the pre-commit baseline** | NOT PROVEN, finding M2 |
| 20 | The vendor script is not loaded twice anywhere | Exactly one loader and one configuration script per page on all nine, zero inline copies remaining, one `site-chrome.js` per page | PASS |
| 21 | `build_dist.py` passes on the current tree (7.5) | Clean build, 37 files, bundle sha256 `67a23f23bbe813bc` | PASS |
| 22 | `build_dist.py` fails on navigation drift (7.5) | Proven three ways on a scratch copy: removing Library from conductor, reordering Library and Plan on team, and adding a visible `Dark` span inside the theme button on faq. All three print the offending page and character offset and exit 1. Restoring returns exit 0 | PASS |
| 23 | The three sign-in pages are untouched (7.6, 6) | `713e3f8` touched none of `login.html`, `staff.html`, `staff-operations.html`. `staff-operations.html` was last changed by `a334250`, the earlier unrelated quarantine commit, as expected. None of the three loads `site-chrome.js`, carries `data-theme-toggle`, or carries `nav.nav-links` | PASS |
| 24 | Navigation lint excludes the sign-in pages (6) | Build output names all three as excluded | PASS |
| 25 | Narrow widths do not break the bar | At 1024, 768 and 390 pixels: nothing hidden, nothing off screen left or right, no horizontal page overflow, theme control still 40 by 40 on all nine | PASS with finding M1 at 390 |
| 26 | The shop still behaves: cart opens, counts, totals | Added one Payment Agent and two Shipping Agent, badge read 3, drawer opened, lines read correctly, drawer totals read Monthly $300.00 per month and Total PV 300 PV | PASS |
| 27 | Pay button legible in both themes | Wallet approve button, white on rgb(24,90,188): 6.51 to 1 in both themes | PASS |
| 28 | Red failure message legible in both themes | Dark `#FCA5A5`: 10.35 on the page field, 9.58 on the summary block. Light `#B42318`: 6.57 on the page field, 6.43 on the summary block | PASS |
| 29 | Console clean on all nine | Zero errors or warnings on all nine, excluding third-party chat vendor noise | PASS |
| 30 | Keyboard reachability and focus | Identical focus order on every page, `aria-label="Primary"` on every navigation, theme control is a real button with a 2 pixel focus-visible ring, the Enroll placeholder is correctly `aria-disabled` and out of the tab order | PASS |
| 31 | Standing row: discoverability, no dead ends | Every navigation control is reachable and does something. The Enroll placeholder declares its own state with a "soon" pill rather than refusing silently. faq, product and library-agent are each reachable by a real link | PASS |
| 32 | Documentation truth: no false status claim in a document this round touched | No project document claims a passed gate or a deploy. Two factual slips in the commit record, findings M3 and L4 | PASS on status, see M3 and L4 |

---

## 3. Findings

### MEDIUM

**M1. The bar's item list is shared but its stylesheet is not, and the Plan page already
looks different. Pages: comp-plan.html primarily, conductor.html structurally.**

The markup is unified by `_partials/nav.html` and the build lint, and the behaviour is
unified by `site-chrome.js`. The chrome's **presentation** is still hand-rolled in four
separate places:

| Page | Where the navigation CSS comes from |
|---|---|
| index, shop, product, team, faq | `css/corporate.css` |
| library, library-agent | `css/library.css` |
| comp-plan | its own inline `<style>` block, no shared stylesheet at all |
| conductor | its own inline `<style>` block, no shared stylesheet at all |

`comp-plan.html` and `conductor.html` each define `.nav-links`, `.nav-link`,
`.nav-link.is-active`, `.nav-link.is-soon`, `.nav-signin`, `.nav-theme` and its
focus ring inline, in full.

Measured consequence today, at 1280 pixels wide:

| Page | Navigation font | Item gap | Header height | Navigation width |
|---|---|---|---|---|
| index, conductor, team, faq | 13.44 px | 26 px | 69 px | 898 px |
| library, library-agent | 13.44 px | 24 px | 69 px | 878 px |
| shop, product | 13.44 px | 26 px | 105 px | 953 px with cart |
| **comp-plan** | **12.80 px** | **20 px** | **65 px** | **796 px** |

At 390 pixels wide the difference is not subtle. Eight pages centre a 335 pixel bar
across the screen. `comp-plan` packs the same items left-aligned into a 181 pixel block
that starts at x=166 and wraps into four cramped rows, because its inline copy sets
`justify-content: normal` where the shared sheet sets `center`.

Nothing is hidden and nothing overflows, so this is not a broken bar. It is the same
bar rendered at a different size, spacing and alignment on one of the nine pages, which
is precisely the complaint that produced this contract. `conductor.html` currently
measures identical to index, which is the more worrying half: it matches today, by
hand, with nothing enforcing it. Contract section 4 says a build that cannot drift is
worth more than a navigation that is correct today, and no lint covers CSS drift.

*Recommended:* have `comp-plan.html` and `conductor.html` consume the shared chrome
rules instead of copying them, or at minimum bring comp-plan's font size, gap and
mobile justification into line, and extend the lint to the chrome CSS.

**M2. The Support button's open path cannot be evidenced, and it gives the user nothing
back if it fails. Pages: all nine.**

What was proven: the trigger exists on all nine, the handler is wired on all nine (a spy
on the vendor's `open` call fires exactly once per click on every page), the vendor
loader and configuration script are each loaded exactly once per page, and no inline
copy of the widget survives anywhere.

What could not be proven: that a chat window actually appears. On every page the vendor
container renders at zero height, `webchat:ready` never fired within 12 seconds, and
calling the vendor's own `open()` directly did nothing. **The pre-commit baseline
behaves identically**, so this is an environment limitation, not a regression this
commit introduced. It does mean the acceptance line "the support button works on all
nine" is graded on wiring, not on an opened chat.

There is also an unproven failure path worth closing. In `site-chrome.js`, `openNow()`
returns true as soon as `window.botpress.open` is a function. That function appears
about 383 milliseconds after load, well before the widget reports ready. If the vendor
proxy does not queue a pre-ready call, a click in that window is swallowed, the
deferred-open fallback is never armed because `wantOpen` is never set, and the button
gives the reader no feedback at all.

*Recommended:* arm the deferred open whenever the widget has not reported ready, rather
than trusting the presence of the function, and give the button a visible pending state
so a slow or blocked widget is never a silent refusal. Before deploy, one person should
open one page in a visible browser and click Support once.

**M3. The round's disclosed list of deliberately dark surfaces omits the largest one.
Pages: all nine.**

The commit record states the deliberately dark surfaces inside the light theme as
"each documented where it lives: the starfield hero, the bank-approval bar, and the
wallet and brand tiles."

Measured: **the navigation bar and the footer also stay dark in the light theme on all
nine pages.** With light active, the header computes `rgba(6, 11, 24, 0.94)` and the
footer `#050914`, identically on every page. This is deliberate and it is documented in
`corporate.css` as "the chrome: dark in both themes, declared once", and it is applied
consistently, so it is not a rendering defect. But it is the biggest thing the light
theme does not change, it is not in the contract's named fallback list, and it is not in
the round's own summary of exceptions. Anyone reading the record would not know that
choosing light leaves the bar and the footer dark.

*Recommended:* add the chrome bar and the footer to the contract's named exceptions and
to the round's record. No code change needed.

### LOW

**L1. Navigation link colour differs on two of nine pages. Pages: shop.html,
product.html.** Navigation links compute `rgb(255,255,255)` on shop and product and
`rgb(199,208,222)` on the other seven, in both themes. Both are comfortably legible
against the dark bar. The bar is simply a shade brighter on the two shop pages.

**L2. The pre-paint theme snippet is hand-written per page, in two different wordings.
Pages: all nine.** The snippet must be inline, so nine copies are unavoidable. Eight
pages carry one wording and `shop.html` carries a different one. Both are synchronous,
both set the attribute before `<body>`, both default to dark on a storage error, and
neither flashes. No lint covers this file-to-file, so it is the next thing positioned
to drift.

**L3. The navigation drift lint waves through a wrong active item. File:
`deploy/build_dist.py`.** Proven on a scratch copy: moving `is-active` and
`aria-current="page"` off Team and onto Shop inside `team.html` left the build passing
with exit code 0. Contract section 4 explicitly authorises ignoring the active-state
difference, so the builders implemented what they were told; this is a hardening note,
not a breach. All nine pages currently mark the correct item. Tightening it to "the
active marker may differ, and it must sit on the item whose href matches this page"
closes the hole.

**L4. Miscount in the commit record.** The commit message says "Five old storage keys
collapse to `orvanna-theme`". Four distinct keys existed before this round
(`orvannaLibraryTheme` shared by library and library-agent, plus
`orvanna-conductor-theme`, `orvanna-comp-plan-theme`, `orvanna-faq-theme`).
`site-chrome.js`'s own comment correctly says four. No user impact.

**L5. Google Pay tile blue letter G at 3.41 to 1.** See the ruling in section 5.
Accepted, with a documentation requirement.

### Measured and explicitly not a defect

- Cart drawer decrement button, light theme, 2.01 to 1. The control is genuinely
  `disabled` at `opacity: 0.35` because the line quantity is 1. WCAG SC 1.4.3 exempts
  inactive controls. Recorded so the number is not mistaken for a miss later.
- Hero title on index, light theme. It is gradient-clipped text on the deliberately
  dark hero. The palest gradient stop, `rgb(165,180,252)` on `rgb(6,11,24)`, computes
  **9.86 to 1**. The white stop computes 19.65 to 1.
- The bank-connect dialog and the wallet sheet keep dark or brand-fixed treatments in
  both themes. Every text pair inside them was measured in both themes and every one
  clears the floor. Lowest inside those surfaces: bank dialog label at 4.86 in light
  and 9.98 in dark.

---

## 4. Contrast, both themes, all nine pages

Full sweep, every text-bearing element, gradients resolved, disabled excluded.
Floor is 4.5 to 1 for normal text and 3 to 1 for large text.

| Page | Elements | Failures dark | Failures light | Worst dark | Worst light |
|---|---|---|---|---|---|
| index.html | 35 | 0 | 0 | 5.68 legal line | 5.68 legal line |
| shop.html | 206 | 0 | 0 | 5.68 legal line | 4.86 PV meter |
| product.html | 55 | 0 | 0 | 5.68 legal line | 5.24 PV badge |
| team.html | 31 | 0 | 0 | 5.68 legal line | 5.68 legal line |
| faq.html | 182 | 0 | 0 | 5.68 legal line | 5.50 category chip |
| comp-plan.html | 865 | 0 | 0 | 5.61 legal line | 4.51 "approved, not built" chip |
| conductor.html | 232 | 0 | 0 | 5.61 note text | 4.67 pull quote |
| library.html | 380 | 0 | 0 | 5.26 "Not yet written" tag | 4.62 "Not yet written" tag |
| library-agent.html | 39 | 0 | 0 | 5.61 breadcrumb separator | 4.83 breadcrumb separator |

Worst pair on the whole property: **4.51 to 1**, the "approved, not built" chip on
comp-plan in light, `rgb(180,83,9)` on `rgb(254,243,199)`. Above the floor with 0.01
to spare, which is thin but passing.

Interactive shop surfaces, measured with the panels genuinely rendered:

| Element | Dark | Light |
|---|---|---|
| Wallet approve button, white on rgb(24,90,188) | 6.51 | 6.51 |
| Wallet red sandbox note | 6.54 | 6.54 |
| Payment failure message | 10.35 field, 9.58 summary | 6.57 field, 6.43 summary |
| Bank dialog title | 18.45 | 17.45 |
| Bank dialog kicker | 10.21 | 5.24 |
| Bank dialog field label | 9.98 | 4.86 |
| Bank bar active step | 14.75 | 17.16 |
| Bank bar inactive step | 11.87 | 12.66 |
| Apple Pay tile | 19.27 | 19.27 |
| PayPal tile | 10.93 | 10.93 |
| Google Pay tile, the word "Pay" | 15.39 | 15.39 |
| Google Pay tile, the letter G | 3.41 | 3.41 |

---

## 5. Ruling on the Google Pay letter G

**Measured independently, not taken from the builder.** `#4285F4` on the tile's
`rgb(248,250,252)` computes **3.41 to 1**. The word "Pay" beside it, `rgb(32,33,36)` on
the same tile, computes **15.38 to 1**. The proposed alternative `#1558C0` computes
**6.29 to 1**. All three of builder B's figures are correct.

**Ruling: accepted as it stands. Not on the argument builder B made.**

The brand-logotype exemption in WCAG SC 1.4.3 does not apply here. That exemption covers
a logotype. This is not the Google Pay logotype: it is page text, the letters G and Pay
set in the site's own typeface at weight 800. Calling site text a logotype would let any
element opt out of contrast by asserting a brand, and that argument should not go on the
record as accepted.

It is accepted for three reasons that do hold:

1. The mark functions as an icon, not as text conveying information. It is
   `aria-hidden="true"`, so no assistive technology reads it. The governing criterion
   for a graphical object is SC 1.4.11 non-text contrast at 3 to 1, and 3.41 clears it.
2. The tile's accessible name is independent of the glyph. A sibling screen-reader-only
   span carries "Google Pay". Nothing about identifying or operating the tile depends on
   resolving the G.
3. The information survives without the glyph. The word "Pay" beside it reads at 15.38,
   the tile sits between Apple Pay and PayPal, and no user who cannot resolve the blue G
   loses the ability to identify or use the control.

**Two conditions attach.** First, record the reasoning in `shop.css` beside the
`.g-blue` rule, in the same measured style as the rest of that sheet, naming 3.41,
naming SC 1.4.11 as the criterion it is judged against, and explicitly stating that the
logotype exemption was considered and rejected. An exception that is not written down is
an exception that gets re-litigated. Second, take `#1558C0` at 6.29 the next time the
tile is touched. It is recognisably Google blue, it clears the text floor outright, and
it ends the argument permanently. That is a cleanup, not a blocker.

This is a LOW finding. It does not hold the gate.

---

## 6. Sign-in areas

Confirmed as required.

- Commit `713e3f8` changed no file named `login.html`, `staff.html` or
  `staff-operations.html`.
- `staff-operations.html` was last changed by `a334250`, the earlier unrelated
  quarantine commit. Expected, and out of scope for this round.
- None of the three loads `site-chrome.js`, carries `data-theme-toggle`, or carries
  `nav class="nav-links"`. They keep their own chrome, as instructed.
- The build's navigation lint names all three as excluded.

Contract section 6 is honoured in full.

---

## 7. What has to happen before this deploys

1. **M1.** Bring `comp-plan.html`'s navigation into line with the other eight, ideally by
   having it and `conductor.html` consume the shared chrome rules rather than inline
   copies. This is the finding that keeps Howard's original complaint alive.
2. **M2.** One person opens one page in a visible browser and clicks Support once, to
   turn checklist row 19 from wired into working. Separately, arm the deferred open on
   readiness rather than on the function existing, and give the button a pending state.
3. **M3.** Add the navigation bar and the footer to the contract's named list of surfaces
   that stay dark inside the light theme.
4. **L5.** Write the Google Pay reasoning into `shop.css` beside the rule.

L1 through L4 can ride along or wait.

Re-gate after those land. Per the standing rule, both gates run again on the exact
changed artifact before anything reaches the live property.

---

## 8. New standing checklist rows earned by this pass

Added permanently, per the rule that every miss makes the checklist longer.

- **Shared markup is not shared chrome.** When a round unifies a component's markup,
  grade whether its CSS is also single-sourced. List every stylesheet and inline
  `<style>` block that defines the component's rules, and measure the component's
  rendered font size, spacing, box and mobile alignment on every page. Identical markup
  rendering at different sizes is still the bar changing between pages. This pass found
  four separate definitions of one navigation.
- **Measure after the transition settles, and load the theme rather than forcing it.**
  Flipping `data-theme` and reading immediately returns mid-transition colours and
  invents defects that are not there. Load each page with the target theme already in
  storage.
- **Resolve gradient backgrounds before claiming a ratio.** An element whose painted
  backdrop is a `background-image` will be measured against the wrong colour if only
  `background-color` is walked. Either resolve the gradient stops or mark the pair
  unresolved. Never report a ratio computed against a backdrop that is not what paints.
- **A wired handler is not a working feature.** When a third-party widget cannot be seen
  to open, say so and name the one-minute human check, rather than passing the row on
  the handler firing.
- **Grade the disclosed exception list against the measured one.** Sweep for every
  surface that does not change with the theme and check each against what the round said
  it was keeping dark. The biggest exception is the one most likely to go unmentioned.
