# Build output: the assembled public site. It has its own git repo inside
# (github.com/howkoz/orvanna.io) and is regenerated by MLM-PILOT/deploy/build_dist.py.
MLM-PILOT/deploy/dist/

# Howard's local key storage. NEVER commit: contains HyperSwitch sandbox secrets.
HyperSwitch/
